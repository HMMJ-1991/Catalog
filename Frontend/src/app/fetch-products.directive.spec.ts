import { FetchProductsDirective } from './fetch-products.directive';

describe('FetchProductsDirective', () => {
  it('should create an instance', () => {
    const directive = new FetchProductsDirective();
    expect(directive).toBeTruthy();
  });
});
